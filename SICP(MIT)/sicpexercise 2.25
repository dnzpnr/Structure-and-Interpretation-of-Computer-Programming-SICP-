(define x (list 1 3 (list 5 7) 9))
(car (cdr (car (cdr (cdr x)))))

(define y (list 7 null))
(car y)

(define z (cons 1 (cons 2 (cons 3 (cons 4 (cons 5 (list 6 7)))))))
(car (cdr (cdr (cdr (cdr (cdr (cdr z)))))))
